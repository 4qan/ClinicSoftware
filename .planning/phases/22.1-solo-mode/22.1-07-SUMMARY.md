<!-- allow: em-dash, hollow-close, hollow-self-char, reframe-conclude -->
---
phase: 22.1
plan: 07
subsystem: tests/solo-mode-workflow
tags: [tests, vitest, solo-mode, end-to-end, fetch-spy, regression-net]
requires: [22.1-01, 22.1-02, 22.1-03, 22.1-04, 22.1-05, 22.1-06]
provides:
  - "src/__tests__/helpers/fetchSpy.ts (installFetchSpy / restoreFetch / getFetchCalls / assertZeroCouchFetch)"
  - "src/__tests__/soloMode.workflow.test.tsx (end-to-end solo workflow regression net)"
  - "Networked tests skipped per D-18 / W7 with HYPHEN-variant TODO comment"
affects:
  - "src/__tests__/login.test.tsx (describe.skip + alias rename)"
  - "src/__tests__/sync.test.tsx (describe.skip on useSyncManager + SyncProvider)"
tech-stack:
  added: []
  patterns:
    - "vi.hoisted({ value }) shared mutable mode flag for vi.mock factories"
    - "vi.doUnmock + vi.resetModules + dynamic import for per-test module unmocking"
    - "globalThis.fetch replaced with rejecting vi.fn() spy + URL pattern detector"
    - "REAL fake-indexeddb-backed PouchDB instead of vi.mock for true regression net (B4)"
key-files:
  created:
    - "src/__tests__/helpers/fetchSpy.ts"
    - "src/__tests__/soloMode.workflow.test.tsx"
  modified:
    - "src/__tests__/login.test.tsx"
    - "src/__tests__/sync.test.tsx"
decisions:
  - "B1 — assert backup format via real exportDatabase().metadata.schemaVersion === 2 (no @/utils/backup mock)"
  - "B2 — render upgrade card via /settings?tab=networking deep-link (Plan 06's resolveInitialCategory)"
  - "B3 — exercise REAL /visit/:id/print route, assert pathname did not redirect to '/'"
  - "B4 — no @/db/pouchdb mock; rely on fake-indexeddb from setup.ts so the fetch spy is meaningful"
  - "W5 — assert SyncIndicator absence via queryByTestId('sync-indicator'), not text matching"
  - "W6 — vi.doUnmock('@/db/localSettings') + dynamic re-import to exercise real getDeploymentMode inference"
  - "W7 — canonical TODO uses HYPHEN ('deferred - re-enable'); SPEC.md em-dash quotation is a doc artifact"
  - "Login form label matchers use exact strings ('Username'/'Password') to disambiguate from 'Show password' aria-label"
metrics:
  duration: "~28 minutes"
  completed: "2026-04-30"
  tasks: 3
  files-changed: 4
  commits:
    - "88e8daa test(22.1-07): add reusable fetchSpy helper for solo-mode zero-fetch assertions"
    - "01ca486 test(22.1-07): skip-wrap networked test suites with HYPHEN-variant TODO (D-18)"
    - "1e3d64a test(22.1-07): add end-to-end solo workflow test (SPEC AC #1-7, #9, #10)"
---

# Phase 22.1 Plan 07: Solo Workflow Test + Fetch Spy + Networked Skips Summary

End-to-end solo regression net: a reusable `fetchSpy` helper (D-17 zero-CouchDB-call assertion), a workflow test that hits SPEC ACs #1-7, #9, #10 against REAL `exportDatabase` + REAL `/visit/:id/print` route + REAL upgrade-card render via `?tab=networking` deep-link + REAL fake-indexeddb PouchDB + REAL `getDeploymentMode` legacy-install inference, and `describe.skip` wrappers on the networked test files with the canonical HYPHEN-variant TODO comment.

## What Shipped

### 1. `src/__tests__/helpers/fetchSpy.ts`

| Export | Purpose |
| ------ | ------- |
| `installFetchSpy()` | Replaces `globalThis.fetch` with a `vi.fn()` that rejects with `'fetch should not be called in solo mode (Phase 22.1 D-17)'`. Idempotent. |
| `restoreFetch()` | Restores the original fetch and clears the internal spy reference. |
| `getFetchCalls()` | Returns `spy.mock.calls` as `unknown[][]` for inspection. |
| `assertZeroCouchFetch()` | Filters `getFetchCalls()` against the URL pattern list and `expect(...).toEqual([])` with the offending URLs in the failure message. |

**URL pattern list (CouchDB-bound detector):** `:5984`, `/_session`, `/_users`, `http://192.168.`, `http://10.`, `https://192.168.`, `https://10.`. Belt-and-suspenders: the rejecting spy ALSO surfaces any unmatched fetch attempt.

### 2. `src/__tests__/soloMode.workflow.test.tsx`

14 tests total — 12 active, 2 `it.skip`:

| # | Test | SPEC AC | Notes |
| - | ---- | ------- | ----- |
| 1 | fresh launch shows username + password only, no URL prompt | #1, #4 (partial) | Asserts placeholders + absence of URL form + 'Change server address' button |
| 2 | login as doctor/doctor123 succeeds and authenticates | #2, #3 | `assertZeroCouchFetch()` after login |
| 3 | SyncIndicator absent (W5 — queryByTestId) | #4 (partial) | testid-based, not text |
| 4 | Header role chip absent | #4 (partial) | `queryAllByText(/^(Doctor\|Nurse)$/)` |
| 5 | Sidebar role label absent | #4 (partial) | `^Dr$` / `^Ns$` |
| 6 | Networking tab disabled with aria-disabled, tabindex=-1, title | #6 (partial) | Confirms 'Sync' label gone |
| 7 | ResetNursePassword absent from Account tab | #4 (partial) | |
| 8 | 'Add a second computer' card renders via ?tab=networking (B2) | #6 | Deep-link bypasses disabled-tab interaction barrier |
| 9 | /medications, /settings, /visit/:id/print reachable (B3) | #5 | Asserts pathname did not redirect to '/' |
| 10 | password change via ChangePassword UI works | #7 | Pre-seeds envelope + session, `assertZeroCouchFetch()` after success toast |
| 11 | exportDatabase().metadata.schemaVersion === 2 (B1) | #10 | REAL backup module, REAL PouchDB |
| 12 | legacy-install inference: couchUrl + no mode → networked tree (W6) | #9 | `vi.doUnmock` + dynamic re-import → real `getDeploymentMode` |
| 13 (skip) | PWA install via Chrome | #12 | Manual checkpoint, not jsdom-testable |
| 14 (skip) | Full clinical workflow patient → visit → print → backup | #13 | Deferred; constituent flows already covered elsewhere |

`assertZeroCouchFetch()` is invoked in 4 paths: fresh-launch, login, route-access, password-change.

### 3. Networked test skips (D-18 / W7)

| File | Describe block | Line | Action |
| ---- | -------------- | ---- | ------ |
| `src/__tests__/login.test.tsx` | `Login Flow (CouchDB auth)` | 39 | `describe.skip` + HYPHEN TODO + alias `NetworkedAuthProvider as AuthProvider` for typecheck |
| `src/__tests__/sync.test.tsx` | `useSyncManager` | 79 | `describe.skip` + HYPHEN TODO |
| `src/__tests__/sync.test.tsx` | `SyncProvider` | 213 | `describe.skip` + HYPHEN TODO |

The solo-mode gating describe (`SyncProvider - solo mode gating (Phase 22.1 D-03)`) at line ~300 is **not** skipped — those 2 tests continue to run and pass.

**Re-enable command:** `git grep -l "TODO(Phase 22.1): networked path deferred" src/__tests__/ | xargs sed -i '' 's/describe\.skip/describe/g'`

## B1 / B2 / B3 / B4 / W5 / W6 / W7 Resolutions

| Code | Issue | Resolution |
| ---- | ----- | ---------- |
| B1 | Backup format check might mock `@/utils/backup` and become a tautology | `exportDatabase()` is the REAL import; assertion is `result.metadata.schemaVersion === 2`. `SCHEMA_VERSION` is private — the public surface is `metadata.schemaVersion`. |
| B2 | 'Add a second computer' card hidden behind disabled tab; clicking is blocked | Navigate to `/ClinicSoftware/settings?tab=networking` via `history.pushState` + `popstate` so `SettingsPage`'s `resolveInitialCategory` initializes `activeCategory='sync'` and renders the card. |
| B3 | SPEC AC #5 explicitly lists `/visit/:id/print` as the marquee role-bypass route | Test pushes `/visit/test-visit-123/print` and asserts pathname contains the route AND is not `'/'` (which is what networked-mode `ProtectedRoute` would do for non-doctor roles). |
| B4 | Mocking `@/db/pouchdb` would let the fetch spy become noise (mock fn vs. real HTTP fetch) | NO `vi.mock` for `@/db/pouchdb`; relies on `fake-indexeddb/auto` from `setup.ts`. The fetch spy now catches anything that escapes the solo gate to a real HTTP request. |
| W5 | Plain text 'sync' would be brittle in DOM checks | `queryByTestId('sync-indicator')` (Plan 05 added the testid). Null = correctly hidden. |
| W6 | Mocking `getDeploymentMode` and toggling `modeRef` would not exercise the real legacy-install inference branch | `vi.doUnmock('@/db/localSettings')` + `vi.resetModules()` + dynamic `import('@/App')` exercises the REAL `localSettings.ts` inference logic with `couchUrl` pre-set and `deploymentMode` unset. |
| W7 | SPEC.md AC #11 quotes the TODO with an em-dash; the project rule forbids em-dashes in source code | Canonical source string is the HYPHEN variant: `deferred - re-enable`. `grep -rF "deferred — re-enable" src/__tests__/` returns empty. SPEC.md is locked and not modified. |

## Deviations from Plan

### Auto-fixed issues

**1. [Rule 1 - Bug] `getByLabelText(/password/i)` matched both the password input and the 'Show password' aria-label on the toggle button**

- **Found during:** Task 3 first vitest run — 9 of 12 tests failed with `Found multiple elements with the text of: /password/i`.
- **Issue:** Regex matchers also match aria-label on the show/hide toggle button.
- **Fix:** Replaced `/username/i` → `'Username'` and `/password/i` → `'Password'` (exact-string matchers) throughout the file via Edit `replace_all`.
- **Files modified:** `src/__tests__/soloMode.workflow.test.tsx`
- **Commit:** `1e3d64a` (incorporated before commit; not a separate commit)

**2. [Rule 1 - Test text vs. AC literal grep] Comments containing the strings `vi.mock('@/utils/backup'` and `vi.mock('@/db/pouchdb'` matched the AC's "should be empty" greps**

- **Found during:** Task 3 acceptance criteria check.
- **Issue:** Plan AC `grep -E "vi\.mock\('@/utils/backup'" ... returns empty` literally matched my "// B1: NO vi.mock(...)" explanatory comments.
- **Fix:** Paraphrased the comments to `// B1: NO mock for the @/utils/backup module.` Same for B4.
- **Files modified:** `src/__tests__/soloMode.workflow.test.tsx`
- **Commit:** `1e3d64a`

### Out of scope (pre-existing, unchanged)

- **`npm run build` fails** with two pre-existing TypeScript errors (`src/__tests__/setup.ts:12` TS2578, `src/utils/passwordHash.ts:37` TS2322). Verified by `git stash && npm run build` against the plan's base. Documented in `deferred-items.md` from Plan 04. `npx tsc --noEmit` (no project refs) passes silently. Vitest tests are unaffected.

## Final Verification

```text
npx vitest run
Test Files  37 passed | 1 skipped (38)
     Tests  383 passed | 16 skipped (399)
    Errors  1 error  (pre-existing PrintVisitPage 'database is closed' unhandled rejection,
                       not introduced by Plan 07; baseline behavior)
```

**Test counts:**
- Active solo workflow tests: 12 (covering SPEC AC #1, #2, #3, #4×3, #5 incl. /visit/:id/print, #6×2, #7, #9, #10)
- `it.skip` workflow checkpoints: 2 (SPEC AC #12 PWA install, #13 full clinical workflow)
- Skipped describes in legacy networked files: 3 (1 in login.test.tsx, 2 in sync.test.tsx) covering ~14 individual tests

**Acceptance grep counts:**
- `assertZeroCouchFetch()` calls in workflow: 4
- `vi.hoisted` in workflow: 1
- `modeRef.value = 'networked'` in workflow: 0 (W6 uses doUnmock, not modeRef)
- `vi.doUnmock('@/db/localSettings')` in workflow: 1
- `vi.mock('@/utils/backup'` in workflow: 0
- `vi.mock('@/db/pouchdb'` in workflow: 0
- `exportDatabase()` calls in workflow: 2 (one in import, one in assertion)
- `result.metadata.schemaVersion` in workflow: 2 (B1 strict assertion)
- `/visit/test-visit-123/print` in workflow: 2 (pushState + assertion)
- `queryByTestId('sync-indicator')` in workflow: 1 (W5)
- `tab=networking` in workflow: 2 (B2 deep-link)
- `name: /add a second computer/i` in workflow: 1 (B2 card render)
- HYPHEN TODO across `src/__tests__/`: 3 (1 in login + 2 in sync)
- Em-dash variant `deferred — re-enable` in `src/__tests__/`: 0
- Minimal-wrap diff (`useCouchAuth.ts`, `useSyncManager.ts`, `ChangePassword.tsx`, `ResetNursePassword.tsx`): empty across this plan AND across all of Phase 22.1 (since base `2d04e3675a606f87a38287d470e464a97881b52e`)

## Self-Check: PASSED

Files verified:
- `src/__tests__/helpers/fetchSpy.ts` — FOUND
- `src/__tests__/soloMode.workflow.test.tsx` — FOUND
- `.planning/phases/22.1-solo-mode/22.1-07-SUMMARY.md` — FOUND (this file)

Commits verified:
- `88e8daa` (Task 1) — FOUND in `git log --oneline`
- `01ca486` (Task 2) — FOUND
- `1e3d64a` (Task 3) — FOUND
