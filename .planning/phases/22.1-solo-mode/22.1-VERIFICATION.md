<!-- allow: em-dash, hollow-close, hollow-self-char, reframe-conclude -->
<!-- Internal verification artifact (audience: Claude executors / orchestrator). -->
---
phase: 22.1-solo-mode
verified: 2026-04-30T23:50:00Z
status: human_needed
score: 9/9 SPEC requirements verified, 11/13 acceptance criteria verified, 2/13 deferred to human
re_verification: false
requirement_coverage:
  SPEC-22.1-1: DELIVERED  # deploymentMode setting + legacy inference
  SPEC-22.1-2: DELIVERED  # solo auth (PBKDF2, doctor/doctor123, zero fetch)
  SPEC-22.1-3: DELIVERED  # SyncProvider not mounted in solo + internal short-circuit
  SPEC-22.1-4: DELIVERED  # all five UI surfaces gated (Networking tab disabled per D-09 override, not removed)
  SPEC-22.1-5: DELIVERED  # ProtectedRoute solo bypass
  SPEC-22.1-6: DELIVERED  # ChangePassword solo branch (no CouchDB)
  SPEC-22.1-7: DELIVERED  # disabled "Add a second computer" card
  SPEC-22.1-8: DELIVERED  # SCHEMA_VERSION=2 unchanged
  SPEC-22.1-9: DELIVERED  # networked path preserved (zero diff to useCouchAuth/useSyncManager)
acceptance_criteria_coverage:
  AC-1_fresh_launch_no_url_prompt: DELIVERED
  AC-2_doctor_login_succeeds: DELIVERED
  AC-3_zero_outbound_fetch: DELIVERED
  AC-4_five_surfaces_absent: PARTIAL  # Networking tab kept-but-disabled per D-09 override (intentional, not a gap)
  AC-5_routes_accessible: DELIVERED
  AC-6_upgrade_card_solo_only: DELIVERED
  AC-7_password_change_no_couchdb: DELIVERED
  AC-8_networked_mode_reload: DELIVERED  # tested via W6 dynamic re-import
  AC-9_legacy_install_inference: DELIVERED
  AC-10_backup_schema_version: DELIVERED
  AC-11_networked_tests_skipped: DELIVERED  # hyphen variant per W7
  AC-12_pwa_install: HUMAN_NEEDED  # cannot be tested in jsdom
  AC-13_full_clinical_workflow: HUMAN_NEEDED  # deferred per D-18 to real hardware
human_verification:
  - test: "Install the app via Chrome's 'Install app' UI on the doctor's machine"
    expected: "Manifest loads, standalone window opens, app boots into solo-mode login screen, login as doctor/doctor123 succeeds"
    why_human: "PWA install flow is browser-UI driven; jsdom cannot simulate it (SPEC AC #12, explicitly deferred via it.skip in soloMode.workflow.test.tsx:312)"
  - test: "Run full clinical workflow on real hardware: register patient -> create visit with vitals -> add medications -> open print preview -> save -> revisit -> export backup -> verify backup file is on disk"
    expected: "All steps complete without error, backup JSON has metadata.schemaVersion === 2, no console errors, no network errors"
    why_human: "End-to-end smoke depends on print preview rendering, file-download mechanics, and IndexedDB durability across reloads — constituent flows are unit-tested but the integrated path is explicitly deferred (SPEC AC #13, it.skip in soloMode.workflow.test.tsx:317; D-18 networked path also deferred to real hardware)"
  - test: "Set localStorage.deploymentMode='networked' on a real machine with the doctor's CouchDB reachable, reload, and verify Phase 22 baseline behavior (URL prompt -> session auth -> sync indicator going green -> role-gated nav -> Reset Nurse Password panel)"
    expected: "Networked path behaves identically to Phase 22 baseline: SyncIndicator visible, role chip visible, Sync tab enabled, ResetNursePassword present, /_session call lands"
    why_human: "Networked test suite is describe.skip per D-18 (16 tests across 3 describes); revalidation requires live CouchDB and physical hardware. SPEC AC #8 is partially proxied by W6 in workflow test (real getDeploymentMode infers 'networked', App boots networked LoginPage with 'doctor or nurse' placeholder), but the live sync handshake is deferred."
---

# Phase 22.1: Solo Mode Verification Report

**Phase Goal:** A non-technical doctor can open ClinicSoftware on a freshly installed machine and use it immediately, with no CouchDB install, no first-run wizard, and no setup decisions, while the existing networked deployment path is preserved as an explicit opt-in upgrade.

**Verified:** 2026-04-30T23:50:00Z
**Status:** human_needed (all programmatic checks pass; 3 human-verifiable items remain — PWA install, full clinical smoke, networked-path real-hardware revalidation)
**Re-verification:** No — initial verification.

## Goal Achievement

The codebase delivers the phase goal. All 9 SPEC requirements are implemented; 11/13 acceptance criteria pass programmatically; the remaining 2 (PWA install + full clinical workflow) are explicitly out-of-jsdom and routed to human verification. Test signal: **383 passed / 16 skipped / 0 failures** (the 16 skips are the intentional D-18 networked-path deferrals; pre-existing PouchDB leveldb unhandled rejection in `PrintVisitPage.test.tsx` is unrelated to 22.1 and matches REVIEW.md baseline notes).

The single SPEC deviation worth flagging is **D-09 (Networking tab kept-but-disabled instead of removed from DOM)**. SPEC AC #4 originally listed the Sync tab button among "absent from DOM"; CONTEXT.md `<spec_lock>` block documents this override explicitly ("SPEC override (this discussion)... the renamed 'Networking' tab button stays in the DOM but disabled"). The implementation matches the override, not the unmodified SPEC text. This is a documented intentional deviation, not a gap.

## Per-Requirement Verdict

### SPEC-22.1-1 — Deployment mode setting (DELIVERED)

**Evidence:**
- `src/db/localSettings.ts:28-36` — `getDeploymentMode()` with three branches: stored canonical value, legacy-install inference (`couchUrl` exists + `deploymentMode` unset → return `'networked'` AND persist), default `'solo'`.
- `src/db/localSettings.ts:38-40` — `setDeploymentMode(mode)` setter.
- Whitelist parser collapses non-canonical values to `'solo'` (line 30 explicit equality check).
- Legacy-install inference verified by `localSettings.test.ts` (11 unit tests passing) AND end-to-end via `soloMode.workflow.test.tsx:273-309` which `vi.doUnmock`s the module and exercises the REAL inference logic (W6).
- Acceptance: setting `deploymentMode='networked'` and re-rendering `<App />` boots into networked tree (placeholder `'doctor or nurse'`) — confirmed line 299.

### SPEC-22.1-2 — Solo-mode authentication (DELIVERED)

**Evidence:**
- `src/auth/useSoloAuth.ts:95-142` — `login()` validates against PBKDF2 envelope in localStorage; first-launch path (line 99-117) accepts hardcoded `doctor`/`doctor123`, hashes immediately, persists envelope.
- `src/utils/passwordHash.ts:7-9, 48` — PBKDF2 SHA-256, 100k iterations (D-05 floor), 16-byte random salt via `crypto.getRandomValues`.
- `src/utils/passwordHash.ts:66-71` — constant-time XOR-OR compare in `verifyPassword`.
- `grep -n "fetch[(]" src/auth/useSoloAuth.ts` returns 0 matches in code (only documentation comments at lines 7, 12). **Zero outbound CouchDB requests.**
- `assertZeroCouchFetch()` invoked in 4 paths in `soloMode.workflow.test.tsx`: fresh-launch (line 68), login (line 84), route-access (line 220), password-change (line 255). URL-pattern detector covers `:5984`, `/_session`, `/_users`, `http://192.168.`, `http://10.`, `https://...` (helpers/fetchSpy.ts).

### SPEC-22.1-3 — Solo-mode sync gating (DELIVERED)

**Evidence (two-gate mitigation, T-22.1-18):**
- **Gate 1 (provider-tree omission):** `src/App.tsx:70-81` — `SoloProviders` component does NOT mount `<SyncProvider>`. `grep -c "<SyncProvider>" src/App.tsx` returns `1` (only inside `NetworkedProviders`, line 86).
- **Gate 2 (effect short-circuit):** `src/sync/SyncContext.tsx:44` — `if (getDeploymentMode() === 'solo') return` is the first statement of the `useEffect`. Same guard at line 62 for the manual `startSync()` retry path.
- `useSyncManager.ts` is unchanged from baseline (`git diff 7abb749^..HEAD -- src/sync/useSyncManager.ts` returns 0 lines), so `pouchDb.sync` cannot be invoked when the gate is in place.
- Workflow test (`soloMode.workflow.test.tsx`) renders App in solo with REAL fake-indexeddb-backed PouchDB and asserts `assertZeroCouchFetch()` — any PouchDB sync attempt would surface as a real fetch.

### SPEC-22.1-4 — Solo-mode UI gating (DELIVERED, with documented D-09 override)

**Evidence (each surface verified):**
- **SyncIndicator absent:** `src/components/Sidebar.tsx:138` — `{!isSolo && <SyncIndicator collapsed={collapsed} />}`. Workflow test line 103: `expect(screen.queryByTestId('sync-indicator')).toBeNull()`.
- **Networking tab (D-09 override):** `src/pages/SettingsPage.tsx:55-77` — tab rendered with `disabled={isSolo && tab.key === 'sync'}`, `aria-disabled`, `tabIndex={-1}`, `title="Networking is not available in solo mode"`, label flips to `'Networking'`. Workflow test lines 144-154 asserts these attributes. **This is the documented SPEC deviation per CONTEXT.md `<spec_lock>` block** — tab stays in DOM, disabled, with renamed label. SPEC AC #4 is partially relaxed; D-09 is the ruling decision.
- **ResetNursePassword absent:** `src/pages/SettingsPage.tsx:95` — `{!isSolo && role === 'doctor' && <ResetNursePassword />}`. Workflow test line 160: `expect(screen.queryByRole('heading', { name: /reset nurse password/i })).toBeNull()`.
- **Header role chip absent:** `src/components/Header.tsx:112` — `{!isSolo && <span...>}`. Workflow test lines 110-111: `screen.queryAllByText(/^(Doctor|Nurse)$/).length === 0`.
- **Sidebar role label absent:** `src/components/Sidebar.tsx:122` — `{!isSolo && role && (...)}` covers both expanded ("Doctor"/"Nurse") and collapsed ("Dr"/"Ns") variants. Workflow test lines 118-119.
- **CouchDB URL form absent:** `src/auth/LoginPage.tsx:75` — `{!isSolo && needsUrl ? URL_FORM : LOGIN_FORM}` PLUS `useEffect` early-returns when `isSolo` (line 18). "Change server address" button gated `{!isSolo && (...)}` at line 163. Workflow test lines 65-67.

### SPEC-22.1-5 — Solo-mode role gating bypass (DELIVERED)

**Evidence:**
- `src/components/ProtectedRoute.tsx:22` — `if (getDeploymentMode() === 'solo') return <>{children}</>`. Auth check (line 18) runs FIRST so unauthenticated users still redirect; W8 always-call-hook pattern keeps the hook at top (line 13). No `eslint-disable` directives.
- Workflow test lines 192-218 navigates `/medications`, `/settings`, `/visit/test-visit-123/print` and asserts `window.location.pathname` matches each route AND is not rewritten to `'/'` (B3 strong assertion).
- `rbac.test.tsx` (21 tests passing including 2 new solo-bypass tests).

### SPEC-22.1-6 — Solo-mode password change (DELIVERED)

**Evidence:**
- `src/auth/useSoloAuth.ts:150-168` — `changePassword()` verifies current password against stored envelope, hashes new password, calls `setSoloCredentials(next)`. Returns UI-SPEC verbatim error string `'Incorrect current password.'` on failure.
- `src/auth/ChangePassword.tsx` — mode-agnostic, calls `useAuthContext().changePassword`. In solo, this resolves to `useSoloAuth.changePassword`. Zero `fetch(` references in either file.
- `git diff 7abb749^..HEAD -- src/auth/ChangePassword.tsx` returns 0 lines (component untouched per minimal-wrap).
- Workflow test lines 224-256 exercises the full UI flow (type current/new/confirm, click "Change Password", await success toast) and asserts `assertZeroCouchFetch()` — covers SPEC AC #7 verbatim.
- `useSoloAuth.test.tsx:159-177` covers wrong-current-password rejection.

### SPEC-22.1-7 — Upgrade UI scaffold (DELIVERED)

**Evidence:**
- `src/pages/SettingsPage.tsx:121-130` — solo branch renders disabled card with title "Add a second computer", body "Coming soon.", and disabled CTA button "Coming soon" (D-13 verbatim).
- Networked branch (line 133) renders `<NetworkedSyncTab />` instead — the upgrade card is solo-only.
- Reachable via `?tab=networking` deep-link (line 23-30, `resolveInitialCategory` whitelist parser).
- Workflow test lines 163-179 navigates to `?tab=networking` and asserts the heading, body text, and disabled button are present, AND the Phase 22 sync status section is absent (`Retry connection`, `Last synced:`).

### SPEC-22.1-8 — Backup format compatibility (DELIVERED)

**Evidence:**
- `src/utils/backup.ts:6` — `const SCHEMA_VERSION = 2` (private module constant, unchanged).
- `git diff 7abb749^..HEAD -- src/utils/backup.ts` returns 0 lines (untouched in 22.1).
- Workflow test lines 259-271 (B1) calls REAL `exportDatabase()` (no `vi.mock('@/utils/backup')`) against REAL fake-indexeddb-backed PouchDB and asserts `result.metadata.schemaVersion === 2`.

### SPEC-22.1-9 — Networked path preservation (DELIVERED)

**Evidence (zero-diff invariant):**
- `git diff 7abb749^..HEAD -- src/auth/useCouchAuth.ts src/sync/useSyncManager.ts` → **0 lines changed**. Both files untouched across the entire phase.
- `git diff 7abb749^..HEAD -- src/auth/ChangePassword.tsx src/auth/ResetNursePassword.tsx` → **0 lines changed**. Mode-agnostic UI components reused without modification.
- `NetworkedAuthProvider` (`AuthProvider.tsx:39-42`) calls `useCouchAuth()` — same hook, same behavior.
- `SyncContext.tsx:39-57` — networked branch unchanged in shape; only the early return at line 44 was added.
- W6 dynamic re-import test (`soloMode.workflow.test.tsx:273-309`) confirms a real `couchUrl` + unset `deploymentMode` → real `getDeploymentMode()` returns `'networked'` → App renders networked LoginPage (`'doctor or nurse'` placeholder) — proving the legacy install boot path resolves to networked code.

## Cross-Cutting Checks

### Networked-path preservation (zero-diff invariant)

| File | Lines changed in 22.1 | Status |
|------|----------------------|--------|
| `src/auth/useCouchAuth.ts` | 0 | UNCHANGED |
| `src/sync/useSyncManager.ts` | 0 | UNCHANGED |
| `src/auth/ChangePassword.tsx` | 0 | UNCHANGED |
| `src/auth/ResetNursePassword.tsx` | 0 | UNCHANGED |
| `src/utils/backup.ts` | 0 | UNCHANGED |

Phase 22 surface is intact. The minimal-wrap SPEC constraint is verifiably satisfied by the diff itself, not by code-review claims.

### Zero-fetch invariant

- `src/auth/useSoloAuth.ts` — 0 `fetch(` calls (only documentation comments).
- `src/auth/ChangePassword.tsx` — 0 `fetch(` calls.
- `src/utils/passwordHash.ts` — 0 `fetch(` calls (uses `crypto.subtle` only).
- `assertZeroCouchFetch()` invoked 4× in `soloMode.workflow.test.tsx` covering: fresh-launch, login, route-access, password-change. URL-shape filter catches anything CouchDB-bound (`:5984`, `/_session`, `/_users`, RFC1918 IPs).
- `useSoloAuth.test.tsx` calls `expectNoFetchCalls()` 12× (once per test).

### Schema compatibility

- `SCHEMA_VERSION = 2` constant in `backup.ts` is private and unchanged.
- `exportDatabase()` produces `BackupFile` with `metadata.schemaVersion === 2` in solo (workflow test line 266).
- No solo-only fields added to the export (no diff to `backup.ts`).
- Networked exports use the same code path (no mode branching in `backup.ts`); restore path is deferred to Phase 23.

### Two-gate sync mitigation (T-22.1-18)

| Gate | Location | Status |
|------|----------|--------|
| 1. Provider-tree omission | `App.tsx:70-81` `SoloProviders` | VERIFIED — no `<SyncProvider>` in solo subtree |
| 2. Effect short-circuit | `SyncContext.tsx:44, 62` | VERIFIED — solo early return on first statement of both `useEffect` and `startSync` |

Either gate alone would prevent replication; both together are belt-and-suspenders.

### Networked tests skipped per D-18 / W7

- `src/__tests__/login.test.tsx:39` — hyphen-variant TODO + `describe.skip` at line 40.
- `src/__tests__/sync.test.tsx:81` — hyphen TODO + `describe.skip` at line 82 (`useSyncManager`).
- `src/__tests__/sync.test.tsx:214` — hyphen TODO + `describe.skip` at line 215 (`SyncProvider`).
- `grep "deferred — re-enable" src/__tests__/` (em-dash variant) → 0 matches.
- Re-enable command: `git grep -l "TODO(Phase 22.1): networked path deferred" src/__tests__/ | xargs sed -i '' 's/describe\.skip/describe/g'`.

### REVIEW.md findings reconciliation

REVIEW.md reported 0 BLOCKERs, 0 HIGH, 2 MEDIUM, 4 LOW. None affect goal achievement:

- **MD-01** (username not bound into PBKDF2) — accepted threat model per CONTEXT D-15 and T-22.1-10 (single-machine PWA). JSDoc clarification is the cheap fix; not blocking.
- **MD-02** (`changePassword` stale-closure footgun) — latent only; no current consumer triggers it.
- **LO-01** through **LO-04** — all cosmetic/defensive; no functional impact.

## Acceptance Criteria Coverage

| # | AC | Status | Evidence |
|---|----|--------|----------|
| 1 | Fresh launch shows username + password only, no URL prompt | DELIVERED | `soloMode.workflow.test.tsx:55-69` |
| 2 | Login as `doctor` / `doctor123` succeeds | DELIVERED | `soloMode.workflow.test.tsx:73-85`, `useSoloAuth.test.tsx` Test 1 |
| 3 | Zero outbound CouchDB requests during login | DELIVERED | 4× `assertZeroCouchFetch()` calls; URL-shape filter |
| 4 | Five UI surfaces absent from DOM | DELIVERED (with D-09 override) | Tab kept-but-disabled per documented override; other 4 surfaces verified absent |
| 5 | `/medications`, `/settings`, `/visit/:id/print` accessible | DELIVERED | `soloMode.workflow.test.tsx:182-222` (B3 strong pathname assertion) |
| 6 | Disabled "Add a second computer" card in solo only | DELIVERED | `soloMode.workflow.test.tsx:163-179`; not rendered in networked branch (`SettingsPage.tsx:133`) |
| 7 | Password change works without CouchDB | DELIVERED | `soloMode.workflow.test.tsx:224-256` |
| 8 | `deploymentMode='networked'` reload reproduces Phase 22 | DELIVERED (programmatic proxy via W6) | `soloMode.workflow.test.tsx:273-309`; live CouchDB handshake deferred to human |
| 9 | `couchUrl` set + `deploymentMode` unset → networked | DELIVERED | `localSettings.test.ts` Test 2 + workflow test W6 |
| 10 | Solo backup `SCHEMA_VERSION` matches networked | DELIVERED | `soloMode.workflow.test.tsx:259-271` (B1 real `exportDatabase`) |
| 11 | Networked-mode tests `.skip` with TODO comment | DELIVERED | hyphen variant at 3 sites (W7) |
| 12 | PWA install via Chrome | HUMAN_NEEDED | `it.skip` at `soloMode.workflow.test.tsx:312` — jsdom cannot test |
| 13 | Full clinical workflow patient → backup smoke | HUMAN_NEEDED | `it.skip` at `soloMode.workflow.test.tsx:317` — deferred per D-18 |

## Anti-Patterns Scan

| File | Concern | Severity | Impact |
|------|---------|----------|--------|
| `src/utils/passwordHash.ts:37` | TS2322 `Uint8Array<ArrayBufferLike>` (pre-existing, blocks `npm run build`) | Info | Documented in `deferred-items.md`; reproduces on baseline before any 22.1 edits; unrelated to 22.1 scope |
| `src/__tests__/setup.ts:12` | TS2578 unused `@ts-expect-error` (pre-existing, blocks `npm run build`) | Info | Same — pre-existing, documented in `deferred-items.md` |
| `useSoloAuth.ts:101` | Hardcoded `doctor`/`doctor123` defaults | Info | Intentional per D-06 (first-launch friction); rotated by ChangePassword |

No TODO/FIXME/HACK markers in 22.1 production files. No hardcoded empty data flowing to UI. No console.log debug leakage in `useSoloAuth.ts` / `passwordHash.ts` / `soloCredentials.ts` (REVIEW.md "Coverage Notes" confirms).

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---------|---------|--------|--------|
| Full test suite green | `npx vitest run` | 383 passed / 16 skipped / 0 failed (1 pre-existing PouchDB teardown unhandled rejection in PrintVisitPage, unrelated) | PASS |
| useCouchAuth zero diff | `git diff 7abb749^..HEAD -- src/auth/useCouchAuth.ts \| wc -l` | 0 | PASS |
| useSyncManager zero diff | `git diff 7abb749^..HEAD -- src/sync/useSyncManager.ts \| wc -l` | 0 | PASS |
| SyncProvider mounted only in networked | `grep -c "<SyncProvider>" src/App.tsx` | 1 | PASS |
| Em-dash TODO absent | `grep "deferred — re-enable" src/__tests__/` | 0 matches | PASS |
| Hyphen TODO present at 3 sites | `grep "deferred - re-enable" src/__tests__/` | 3 matches | PASS |
| Solo auth has zero fetch calls | `grep "fetch[(]" src/auth/useSoloAuth.ts` (excluding comments) | 0 code matches | PASS |
| Solo sync gate first statement | `SyncContext.tsx:44` | `if (getDeploymentMode() === 'solo') return` | PASS |
| Backup schema unchanged | `grep "SCHEMA_VERSION = " src/utils/backup.ts` | `const SCHEMA_VERSION = 2` | PASS |

## Outstanding Items (Human Verification Required)

### 1. PWA install via Chrome (SPEC AC #12)

**Test:** Open `https://4qan.github.io/ClinicSoftware/` (or local dev server) in Chrome on the doctor's machine. Click the "Install app" button in the Chrome address bar. Verify standalone window opens. Log in as `doctor` / `doctor123`. Navigate Home → Patients → Settings.
**Expected:** Manifest loads, standalone window opens, login succeeds, all routes navigable.
**Why human:** PWA install flow is browser-UI-driven; jsdom cannot simulate it. Explicitly `it.skip` at `soloMode.workflow.test.tsx:312`.

### 2. Full clinical workflow smoke (SPEC AC #13)

**Test:** Register patient → create visit with vitals → add medications → open print preview → save → revisit (reload, log back in, open the same patient) → export backup → confirm backup file present on disk (Downloads).
**Expected:** All steps complete, backup JSON has `metadata.schemaVersion === 2`, no console errors, no network errors. Survives reload (IndexedDB durability).
**Why human:** End-to-end smoke depends on print-preview rendering, browser file-download mechanics, and IndexedDB persistence across reloads. Constituent flows are unit-tested individually; the integrated path is `it.skip` at `soloMode.workflow.test.tsx:317` and deferred per D-18.

### 3. Networked-path real-hardware revalidation (SPEC AC #8 live confirmation)

**Test:** On a machine with the doctor's CouchDB reachable: `localStorage.setItem('deploymentMode', 'networked')`, reload, complete URL prompt, log in via CouchDB session auth, verify SyncIndicator turns green, verify role chip + Reset Nurse Password panel are present, verify role-gated nav for nurse account.
**Expected:** Phase 22 baseline reproduced 1:1 — no visible diff from before 22.1 was merged.
**Why human:** D-18 deferred 16 networked tests (`describe.skip`) until real-hardware revalidation. SPEC AC #8 is partially proxied by W6 (real `getDeploymentMode` infers networked, real App boots networked LoginPage) but the live CouchDB handshake — which is the actual user-facing risk — is deferred. Until the doctor's machine is set up, this is the one gap an automated test cannot close.

## Gaps Summary

**No code gaps.** All 9 SPEC requirements are implemented with verifiable code-location evidence and full test coverage where the test environment permits.

The only "deviations" are documented intentional ones: D-09 keeps the Networking tab in the DOM (disabled) instead of removing it per SPEC AC #4, and D-18 defers networked-mode tests to real hardware. Both are documented in CONTEXT.md `<spec_lock>` and `<decisions>`, traced through the plans, and reflected in the implementation.

The remaining outstanding items are inherent limits of jsdom + the absence of a real CouchDB on the verifier's machine, not implementation gaps. The orchestrator should route these to user verification before declaring the phase shipped on the doctor's machine.

---

_Verified: 2026-04-30T23:50:00Z_
_Verifier: Claude (gsd-verifier)_
_Test signal: 383 passed / 16 skipped / 0 failed (Vitest)_
_Minimal-wrap diff: 0 lines on `useCouchAuth.ts`, `useSyncManager.ts`, `ChangePassword.tsx`, `ResetNursePassword.tsx`, `backup.ts`_
