---
phase: 11-layout-scaling-preview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/printSettings.ts
  - src/components/PrescriptionSlip.tsx
  - src/index.css
  - src/__tests__/printSettings.db.test.ts
  - src/__tests__/PrintVisitPage.test.tsx
  - src/__tests__/PrescriptionSlip.test.tsx
autonomous: true
requirements: [SCALE-01, SCALE-03]

must_haves:
  truths:
    - "A6 is no longer a selectable paper size anywhere in the app"
    - "A stored A6 value in IndexedDB silently falls back to A5"
    - "Prescription slip font sizes visibly differ between A5 and A4"
    - "Urdu text on prescription slip has per-size line-height (not hardcoded 2.2)"
  artifacts:
    - path: "src/db/printSettings.ts"
      provides: "calcScale(), URDU_LINE_HEIGHTS, A6-free PaperSize type"
      exports: ["calcScale", "URDU_LINE_HEIGHTS", "PaperSize", "PAPER_SIZES"]
    - path: "src/components/PrescriptionSlip.tsx"
      provides: "Scaled prescription slip accepting paperSize prop"
      contains: "calcScale"
    - path: "src/index.css"
      provides: "Dynamic Urdu line-height via CSS custom property"
      contains: "--urdu-line-height"
    - path: "src/__tests__/PrescriptionSlip.test.tsx"
      provides: "Tests for SCALE-01 and SCALE-03"
  key_links:
    - from: "src/components/PrescriptionSlip.tsx"
      to: "src/db/printSettings.ts"
      via: "calcScale(paperSize) for font sizing"
      pattern: "calcScale\\("
    - from: "src/index.css"
      to: "src/components/PrescriptionSlip.tsx"
      via: "--urdu-line-height CSS custom property"
      pattern: "var\\(--urdu-line-height"
---

<objective>
Remove A6 paper size, add scaling infrastructure (calcScale, URDU_LINE_HEIGHTS), and make PrescriptionSlip scale proportionally to the selected paper size.

Purpose: Establish the scaling pattern and apply it to the prescription slip so content fills A4/Letter pages instead of rendering at fixed A5 size.
Output: A6-free data layer with calcScale(), scaled PrescriptionSlip, per-size Urdu line-height.
</objective>

<execution_context>
@/Users/furqantariq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/furqantariq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-layout-scaling-preview/11-CONTEXT.md
@.planning/phases/11-layout-scaling-preview/11-RESEARCH.md
@.planning/phases/10-print-infrastructure-settings/10-01-SUMMARY.md

<interfaces>
<!-- From src/db/printSettings.ts (current, will be modified) -->
```typescript
export type PaperSize = 'A6' | 'A5' | 'A4' | 'Letter'
export interface PaperDimensions { width: number; height: number; label: string }
export interface PrintSettings { prescriptionSize: PaperSize; dispensarySize: PaperSize }
export const PAPER_SIZES: Record<PaperSize, PaperDimensions>
export const PAPER_SIZE_ORDER: PaperSize[]
export function calcMargin(size: PaperSize): number
export async function getPrintSettings(): Promise<PrintSettings>
export async function savePrintSetting(key: string, value: PaperSize): Promise<void>
```

<!-- From src/components/PrescriptionSlip.tsx (current props) -->
```typescript
interface PrescriptionSlipProps {
  visit: Visit
  medications: VisitMedication[]
  patient: Patient
  clinicInfo: ClinicInfo
  // paperSize will be added
}
```

<!-- From src/index.css (current .urdu-cell) -->
```css
@media print {
  .urdu-cell {
    font-family: 'Noto Nastaliq Urdu Variable', serif;
    direction: rtl;
    line-height: 2.2;
    overflow: visible;
    padding-top: 4px;
    padding-bottom: 4px;
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove A6 and add scaling infrastructure</name>
  <files>src/db/printSettings.ts, src/__tests__/printSettings.db.test.ts, src/__tests__/PrintVisitPage.test.tsx</files>
  <behavior>
    - calcScale('A5') returns 1.0 (baseline)
    - calcScale('A4') returns 210/148 (~1.419)
    - calcScale('Letter') returns 216/148 (~1.459)
    - URDU_LINE_HEIGHTS['A5'] is 2.2 (confirmed value)
    - URDU_LINE_HEIGHTS['A4'] and URDU_LINE_HEIGHTS['Letter'] are 2.6 (starting estimates)
    - PaperSize type no longer includes 'A6'
    - PAPER_SIZES no longer has A6 entry
    - PAPER_SIZE_ORDER no longer includes A6
    - getPrintSettings() returns A5 when DB has 'A6' stored
    - Existing PrintVisitPage A6 badge test replaced with A6-fallback-to-A5 test
  </behavior>
  <action>
    1. In `src/db/printSettings.ts`:
       - Remove 'A6' from `PaperSize` type: `export type PaperSize = 'A5' | 'A4' | 'Letter'`
       - Remove A6 entry from `PAPER_SIZES` constant
       - Remove A6 from `PAPER_SIZE_ORDER`: `['A5', 'A4', 'Letter']`
       - Add coercion in `getPrintSettings()`: after reading DB values, check if value is a valid PaperSize (in PAPER_SIZE_ORDER or check explicitly). If not (e.g. 'A6' or garbage), default to 'A5'. Use a helper: `const VALID_SIZES: PaperSize[] = ['A5', 'A4', 'Letter']; function coerceSize(raw: unknown): PaperSize { return VALID_SIZES.includes(raw as PaperSize) ? (raw as PaperSize) : 'A5' }`
       - Add `calcScale()`: `const A5_WIDTH = 148; export function calcScale(size: PaperSize): number { return PAPER_SIZES[size].width / A5_WIDTH }`
       - Add `URDU_LINE_HEIGHTS`: `export const URDU_LINE_HEIGHTS: Record<PaperSize, number> = { A5: 2.2, A4: 2.6, Letter: 2.6 }` with a comment that A4/Letter values are starting estimates needing empirical print testing

    2. In `src/__tests__/printSettings.db.test.ts`:
       - Remove any tests referencing A6 (calcMargin A6 test, A6 in PAPER_SIZES tests, etc.)
       - Add tests: calcScale returns correct values for each size, URDU_LINE_HEIGHTS has entries for each PaperSize
       - Add test: getPrintSettings returns A5 when DB contains 'A6' (save 'A6' directly to DB via db.settings.put, then call getPrintSettings, assert both sizes are A5 or whichever was A6)

    3. In `src/__tests__/PrintVisitPage.test.tsx`:
       - Find the test around line 237 that saves 'A6' as dispensary size and asserts 'Paper: A6 (105 x 148 mm)'. Replace it with a test that saves 'A6' as dispensary size, navigates to print page, and asserts the badge shows A5 (fallback behavior). The test name should be "shows A5 badge when stored size is A6 (fallback)"
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/printSettings.db.test.ts src/__tests__/PrintVisitPage.test.tsx</automated>
  </verify>
  <done>A6 removed from type/constants/order. getPrintSettings coerces A6 to A5. calcScale and URDU_LINE_HEIGHTS exported and tested. PrintVisitPage A6 test updated to verify fallback. All tests pass with no TypeScript errors referencing A6.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scale PrescriptionSlip with paper size prop</name>
  <files>src/components/PrescriptionSlip.tsx, src/__tests__/PrescriptionSlip.test.tsx, src/index.css</files>
  <behavior>
    - PrescriptionSlip rendered with paperSize='A5' has fontSize '11.0pt' (11 * 1.0)
    - PrescriptionSlip rendered with paperSize='A4' has fontSize '15.6pt' (11 * 1.419, rounded to 1 decimal)
    - PrescriptionSlip with paperSize='A4' has header fontSize '19.9pt' (14 * 1.419)
    - PrescriptionSlip root element has maxWidth matching PAPER_SIZES[paperSize].width in mm
    - PrescriptionSlip root element has CSS custom property --urdu-line-height matching URDU_LINE_HEIGHTS[paperSize]
    - .urdu-cell in CSS reads var(--urdu-line-height, 2.2) instead of hardcoded 2.2
  </behavior>
  <action>
    1. Create `src/__tests__/PrescriptionSlip.test.tsx`:
       - Import PrescriptionSlip, create mock props (visit, medications with at least one Urdu instruction, patient, clinicInfo)
       - Test: renders with A5 paperSize, root element has `fontSize: '11.0pt'`, `maxWidth: '148mm'`
       - Test: renders with A4 paperSize, root element has fontSize matching `(11 * calcScale('A4')).toFixed(1) + 'pt'`, maxWidth '210mm'
       - Test: renders with A4 paperSize, h1 (doctor name) has fontSize matching `(14 * calcScale('A4')).toFixed(1) + 'pt'`
       - Test: root element has `--urdu-line-height` CSS custom property set to URDU_LINE_HEIGHTS[paperSize] (check style attribute string or use getComputedStyle)
       - Test: renders with Letter paperSize, values scale correctly

    2. In `src/components/PrescriptionSlip.tsx`:
       - Add `paperSize: PaperSize` to `PrescriptionSlipProps` interface (import PaperSize, calcScale, PAPER_SIZES, URDU_LINE_HEIGHTS from '@/db/printSettings')
       - At top of component: `const scale = calcScale(paperSize)`, `const basePt = +(11 * scale).toFixed(1)`, `const headerPt = +(14 * scale).toFixed(1)`
       - Update root div style: `maxWidth: \`${PAPER_SIZES[paperSize].width}mm\``, `fontSize: \`${basePt}pt\``, add `'--urdu-line-height': URDU_LINE_HEIGHTS[paperSize]` (cast as React.CSSProperties)
       - Update h1 doctor name: `style={{ fontSize: \`${headerPt}pt\` }}`
       - All other inline fontSize references (text-sm, text-xs etc. via Tailwind classes) are relative and will scale with the root fontSize. However, any hardcoded pt/px values in inline styles need to be scaled too. Audit all `style={{ fontSize: ... }}` in the component and scale them by the same factor. The p-6 padding should also scale: replace `className="p-6"` with `style={{ padding: \`${Math.round(24 * scale)}px\` }}` or keep p-6 if Tailwind classes don't need dynamic values (p-6 = 1.5rem which is relative to root font, so it won't auto-scale with pt-based fontSize). Use inline padding: `padding: \`${(6 * scale).toFixed(1)}mm\`` for the main content wrapper.

    3. In `src/index.css`:
       - Change `.urdu-cell` line-height from hardcoded `2.2` to `var(--urdu-line-height, 2.2)`
       - This applies in both the `@media print` block and any screen rules for .urdu-cell
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/PrescriptionSlip.test.tsx src/__tests__/printSettings.db.test.ts</automated>
  </verify>
  <done>PrescriptionSlip accepts paperSize prop. Font sizes scale by width-ratio from A5 baseline. Urdu line-height is dynamic via CSS custom property. maxWidth matches selected paper width. All new and existing tests pass.</done>
</task>

</tasks>

<verification>
npx vitest run src/__tests__/printSettings.db.test.ts src/__tests__/PrescriptionSlip.test.tsx src/__tests__/PrintVisitPage.test.tsx
</verification>

<success_criteria>
- A6 completely removed from types, constants, and UI
- calcScale() and URDU_LINE_HEIGHTS exported and tested
- PrescriptionSlip renders with scaled fonts at each paper size
- .urdu-cell uses dynamic line-height via CSS custom property
- All tests green including updated PrintVisitPage tests
</success_criteria>

<output>
After completion, create `.planning/phases/11-layout-scaling-preview/11-01-SUMMARY.md`
</output>
