---
phase: 05.1-prescription-entry-cleanup
plan: 01
subsystem: database, ui
tags: [dexie, indexeddb, migration, typescript, rename]

requires: []
provides:
  - "VisitMedication.quantity field (renamed from dosage)"
  - "Dexie v3 migration for existing IndexedDB records"
  - "MedicationFormData.quantity field"
  - "MedicationForInstruction.quantity field"
affects: [05.1-prescription-entry-cleanup, 06-rx-notes-urdu-toggle]

tech-stack:
  added: []
  patterns:
    - "Dexie version upgrade with .upgrade() callback for property renames"

key-files:
  created: []
  modified:
    - src/db/index.ts
    - src/components/MedicationEntry.tsx
    - src/constants/translations.ts
    - src/components/MedicationList.tsx
    - src/components/VisitCard.tsx
    - src/components/PrescriptionSlip.tsx
    - src/components/DispensarySlip.tsx
    - src/pages/NewVisitPage.tsx
    - src/pages/EditVisitPage.tsx
    - src/__tests__/visits.test.ts
    - src/__tests__/PrintVisitPage.test.tsx
    - src/constants/__tests__/translations.test.ts

key-decisions:
  - "Dexie v3 stores identical to v2 (no index changes); only upgrade callback renames data property"

patterns-established:
  - "Dexie migration pattern: same .stores() + .upgrade() for non-indexed property renames"

requirements-completed: [RX-CLEANUP-03]

duration: 2min
completed: 2026-03-06
---

# Phase 5.1 Plan 01: Dosage-to-Quantity Rename Summary

**Renamed `dosage` field to `quantity` across 3 interfaces, 9 production files, 3 test files, with Dexie v3 migration for existing IndexedDB records**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-06T19:07:57Z
- **Completed:** 2026-03-06T19:10:00Z
- **Tasks:** 2
- **Files modified:** 12

## Accomplishments
- Renamed `dosage` to `quantity` in VisitMedication, MedicationFormData, and MedicationForInstruction interfaces
- Added Dexie v3 migration that renames property on existing IndexedDB records (no index changes)
- Updated all consumers: MedicationEntry, MedicationList, VisitCard, PrescriptionSlip, DispensarySlip, NewVisitPage, EditVisitPage
- Updated all test fixtures (56 tests pass)
- Zero TypeScript errors, zero stale `dosage` references outside migration callback

## Task Commits

Each task was committed atomically:

1. **Task 1: Rename dosage to quantity in data model, interfaces, and Dexie migration** - `69a0714` (feat)
2. **Task 2: Update all test files for dosage-to-quantity rename** - `3c85b86` (test)

## Files Created/Modified
- `src/db/index.ts` - VisitMedication interface + Dexie v3 migration
- `src/components/MedicationEntry.tsx` - MedicationFormData interface + form state
- `src/constants/translations.ts` - MedicationForInstruction interface + buildUrduInstruction
- `src/components/MedicationList.tsx` - Display references
- `src/components/VisitCard.tsx` - Display references
- `src/components/PrescriptionSlip.tsx` - Instruction building
- `src/components/DispensarySlip.tsx` - Instruction building
- `src/pages/NewVisitPage.tsx` - Visit creation mapping
- `src/pages/EditVisitPage.tsx` - Visit load + save mappings
- `src/__tests__/visits.test.ts` - Test fixtures
- `src/__tests__/PrintVisitPage.test.tsx` - Test fixtures
- `src/constants/__tests__/translations.test.ts` - Test fixtures

## Decisions Made
- Dexie v3 .stores() kept identical to v2; only the .upgrade() callback renames the data property. This is correct because `dosage`/`quantity` was never an indexed field.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `quantity` field is now the canonical name across the entire codebase
- Pre-existing login.test.tsx failures are unrelated (test environment issue with Password label)
- Ready for subsequent 05.1 plans

---
*Phase: 05.1-prescription-entry-cleanup*
*Completed: 2026-03-06*
