---
phase: 05.1-prescription-entry-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/index.ts
  - src/components/MedicationEntry.tsx
  - src/components/MedicationList.tsx
  - src/components/VisitCard.tsx
  - src/components/PrescriptionSlip.tsx
  - src/components/DispensarySlip.tsx
  - src/constants/translations.ts
  - src/pages/NewVisitPage.tsx
  - src/pages/EditVisitPage.tsx
  - src/__tests__/visits.test.ts
  - src/__tests__/PrintVisitPage.test.tsx
  - src/constants/__tests__/translations.test.ts
  - src/constants/translations.test.ts
autonomous: true
requirements:
  - RX-CLEANUP-03

must_haves:
  truths:
    - "VisitMedication interface uses `quantity` field, not `dosage`"
    - "Existing database records with `dosage` are migrated to `quantity` on DB open"
    - "All tests pass with the renamed field"
    - "Prescription and dispensary print slips render correctly with `quantity`"
  artifacts:
    - path: "src/db/index.ts"
      provides: "VisitMedication with quantity field + Dexie v3 migration"
      contains: "quantity: string"
    - path: "src/components/MedicationEntry.tsx"
      provides: "MedicationFormData with quantity field"
      contains: "quantity: string"
    - path: "src/constants/translations.ts"
      provides: "MedicationForInstruction with quantity field"
      contains: "quantity: string"
  key_links:
    - from: "src/db/index.ts"
      to: "visitMedications table"
      via: "version(3).upgrade() callback"
      pattern: "med\\.quantity = med\\.dosage"
    - from: "src/pages/NewVisitPage.tsx"
      to: "src/db/index.ts"
      via: "visitMedication creation"
      pattern: "quantity: med\\.quantity"
    - from: "src/constants/translations.ts"
      to: "src/components/PrescriptionSlip.tsx"
      via: "buildUrduInstruction"
      pattern: "med\\.quantity"
---

<objective>
Rename the `dosage` field to `quantity` across the entire data model, all consumers, and all tests. Add a Dexie v3 migration that renames the property on existing IndexedDB records.

Purpose: The `dosage` field stores quantity (e.g., "1", "5 ml"), not traditional dosage. This rename fixes the misleading name before any further work builds on it.
Output: All 13 files updated, Dexie migration in place, all tests green.
</objective>

<execution_context>
@/Users/furqantariq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/furqantariq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-prescription-entry-cleanup/05.1-RESEARCH.md

<interfaces>
<!-- Current interfaces that will be modified -->

From src/db/index.ts:
```typescript
export interface VisitMedication {
  id: string
  visitId: string
  drugId?: string
  brandName: string
  saltName: string
  form: string
  strength: string
  dosage: string      // RENAME to quantity
  frequency: string
  duration: string
  sortOrder: number
}
```

From src/components/MedicationEntry.tsx:
```typescript
export interface MedicationFormData {
  drugId?: string
  brandName: string
  saltName: string
  form: string
  strength: string
  dosage: string      // RENAME to quantity
  frequency: string
  duration: string
}
```

From src/constants/translations.ts:
```typescript
export interface MedicationForInstruction {
  form: string
  dosage: string      // RENAME to quantity
  frequency: string
  duration: string
}
```

Current Dexie version is 2. Version 3 adds upgrade callback only (no index changes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename dosage to quantity in data model, interfaces, and Dexie migration</name>
  <files>
    src/db/index.ts,
    src/components/MedicationEntry.tsx,
    src/constants/translations.ts,
    src/components/MedicationList.tsx,
    src/components/VisitCard.tsx,
    src/components/PrescriptionSlip.tsx,
    src/components/DispensarySlip.tsx,
    src/pages/NewVisitPage.tsx,
    src/pages/EditVisitPage.tsx
  </files>
  <action>
    Rename `dosage` to `quantity` in all production code. The complete change list (from RESEARCH.md):

    1. **src/db/index.ts**: Rename `VisitMedication.dosage` to `quantity`. Add version(3) with SAME .stores() as v2 (no index changes; `dosage`/`quantity` is not indexed). Add .upgrade() callback:
       ```
       .upgrade(tx => {
         return tx.table('visitMedications').toCollection().modify(med => {
           if ('dosage' in med && !('quantity' in med)) {
             med.quantity = med.dosage
             delete med.dosage
           }
         })
       })
       ```

    2. **src/components/MedicationEntry.tsx**: Rename `MedicationFormData.dosage` to `quantity`. Update `emptyForm` (dosage: '' -> quantity: ''). Update `isValid` check (form.dosage -> form.quantity). Update Qty ComboBox value/onChange (form.dosage -> form.quantity). Update Form ComboBox onChange that resets dosage (dosage: '' -> quantity: '').

    3. **src/constants/translations.ts**: Rename `MedicationForInstruction.dosage` to `quantity`. Update `buildUrduInstruction` to read `med.quantity`. Update `buildDosageUrdu`/`buildDosageEnglish` calls.

    4. **src/components/MedicationList.tsx**: `med.dosage` -> `med.quantity` in formatDosageDisplay calls.

    5. **src/components/VisitCard.tsx**: `med.dosage` -> `med.quantity` in formatDosageDisplay calls.

    6. **src/components/PrescriptionSlip.tsx**: `med.dosage` -> `med.quantity` in buildUrduInstruction call.

    7. **src/components/DispensarySlip.tsx**: `med.dosage` -> `med.quantity` in buildUrduInstruction call.

    8. **src/pages/NewVisitPage.tsx**: `dosage: med.dosage` -> `quantity: med.quantity` in createVisit mapping.

    9. **src/pages/EditVisitPage.tsx**: `dosage: m.dosage` -> `quantity: m.quantity` in both load and save mappings.

    IMPORTANT: Do NOT add `quantity` as an index in .stores(). The stores definition stays identical to v2. Only the upgrade callback renames the data property.
  </action>
  <verify>
    <automated>cd /Users/furqantariq/Documents/Projects/ClinicSoftware && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All 9 production files use `quantity` instead of `dosage`. Dexie v3 migration handles existing records. TypeScript compiles with zero errors. No remaining references to `dosage` in production code (except the migration callback that reads old records).</done>
</task>

<task type="auto">
  <name>Task 2: Update all test files for dosage-to-quantity rename</name>
  <files>
    src/__tests__/visits.test.ts,
    src/__tests__/PrintVisitPage.test.tsx,
    src/constants/__tests__/translations.test.ts,
    src/constants/translations.test.ts
  </files>
  <action>
    Update all test fixtures and assertions that reference `dosage` to use `quantity` instead.

    1. **src/__tests__/visits.test.ts**: Change all `dosage:` in test fixture objects to `quantity:`. Lines ~28, 71, 89, 99, 126, 201, 209.

    2. **src/__tests__/PrintVisitPage.test.tsx**: Change any `dosage:` in medication fixture objects to `quantity:`.

    3. **src/constants/__tests__/translations.test.ts**: Change any `dosage:` references to `quantity:` in test fixtures that create MedicationForInstruction objects.

    4. **src/constants/translations.test.ts**: Change any `dosage:` references to `quantity:` in test fixtures.

    After updating, run the full test suite to confirm all tests pass.
  </action>
  <verify>
    <automated>cd /Users/furqantariq/Documents/Projects/ClinicSoftware && npx vitest run --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>All test files use `quantity` instead of `dosage`. Full test suite passes. Zero grep hits for `dosage` in test files (except potential test descriptions mentioning the rename itself).</done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- all tests pass
2. `grep -r "\.dosage" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v "med.dosage" | grep -v upgrade` -- no stale references outside migration
3. TypeScript compilation: `npx tsc --noEmit` -- zero errors
</verification>

<success_criteria>
- `dosage` field renamed to `quantity` in all 3 interfaces (VisitMedication, MedicationFormData, MedicationForInstruction)
- Dexie v3 migration renames property on existing records
- All ~41+ existing tests pass with the renamed field
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-prescription-entry-cleanup/05.1-01-SUMMARY.md`
</output>
