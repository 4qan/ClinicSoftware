---
phase: 05.1-prescription-entry-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["05.1-01"]
files_modified:
  - src/components/MedicationEntry.tsx
  - src/components/ComboBox.tsx
autonomous: false
requirements:
  - RX-CLEANUP-01
  - RX-CLEANUP-02
  - RX-CLEANUP-04

must_haves:
  truths:
    - "After selecting a DB drug, the input field shows only the brand name"
    - "Dropdown search results still show full detail (brand + salt + strength + form)"
    - "Typing a non-standard value in Qty, Frequency, or Duration shows an amber border"
    - "The amber indicator is informational only; input is never blocked or disabled"
    - "Selecting a predefined option removes the amber indicator"
  artifacts:
    - path: "src/components/MedicationEntry.tsx"
      provides: "Split drug display functions"
      contains: "formatDrugSearchResult"
    - path: "src/components/ComboBox.tsx"
      provides: "Amber custom value indicator"
      contains: "showCustomIndicator"
  key_links:
    - from: "src/components/MedicationEntry.tsx"
      to: "handleSelectDrug"
      via: "formatDrugSelected sets drugQuery to brand name only"
      pattern: "formatDrugSelected"
    - from: "src/components/MedicationEntry.tsx"
      to: "src/components/ComboBox.tsx"
      via: "showCustomIndicator prop on Qty, Frequency, Duration ComboBoxes"
      pattern: "showCustomIndicator"
---

<objective>
Split drug display into search-result vs. selected variants (brand name only after selection), and add amber visual indicator to ComboBox for non-standard values.

Purpose: Eliminate information duplication in the drug field after selection, and give doctors a subtle visual cue when their typed value won't translate to Urdu.
Output: Cleaner drug display UX + guided-with-override pattern on Qty/Frequency/Duration fields.
</objective>

<execution_context>
@/Users/furqantariq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/furqantariq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.1-prescription-entry-cleanup/05.1-RESEARCH.md
@.planning/phases/05.1-prescription-entry-cleanup/05.1-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, MedicationEntry uses `quantity` not `dosage` -->

From src/components/ComboBox.tsx (current props):
```typescript
interface ComboBoxProps {
  options: string[]
  value: string
  onChange: (value: string) => void
  placeholder?: string
  label?: string
  error?: string
  disabled?: boolean
}
```

From src/components/MedicationEntry.tsx (current function):
```typescript
function formatDrugDisplay(drug: Drug): string {
  const parts = [drug.brandName]
  const details: string[] = []
  if (drug.saltName) details.push(drug.saltName)
  if (drug.strength) details.push(drug.strength)
  if (drug.form) details.push(drug.form)
  if (details.length > 0) parts.push(`(${details.join(' ')})`)
  return parts.join(' ')
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split drug display and add ComboBox amber indicator</name>
  <files>src/components/MedicationEntry.tsx, src/components/ComboBox.tsx</files>
  <action>
    **ComboBox.tsx changes:**

    1. Add `showCustomIndicator?: boolean` to ComboBoxProps interface.
    2. Destructure the new prop (default false).
    3. Compute `isCustomValue`:
       ```
       const isCustomValue = showCustomIndicator && value.trim() !== '' &&
         !options.some(opt => opt.toLowerCase() === value.toLowerCase())
       ```
       Case-insensitive comparison per research pitfall #3.
    4. Update input border class: replace `'border-gray-200'` with `isCustomValue ? 'border-amber-400' : 'border-gray-200'` (keep `error` taking precedence).
    5. Add hint text below input, before error message:
       ```
       {isCustomValue && (
         <p className="mt-1 text-xs text-amber-600">Custom value</p>
       )}
       ```
    6. Do NOT add any `disabled` or blocking behavior. The input always accepts any text.

    **MedicationEntry.tsx changes:**

    1. Replace `formatDrugDisplay()` with two functions:
       - `formatDrugSearchResult(drug: Drug): string` -- same logic as current formatDrugDisplay (full detail for dropdown).
       - `formatDrugSelected(drug: Drug): string` -- returns `drug.brandName` only.
    2. In `handleSelectDrug`: change `setDrugQuery(formatDrugDisplay(drug))` to `setDrugQuery(formatDrugSelected(drug))`.
    3. In dropdown rendering (the map over drugResults): change `formatDrugDisplay(drug)` to `formatDrugSearchResult(drug)`.
    4. Add `showCustomIndicator` prop to the three ComboBox instances for Qty, Frequency, and Duration. Do NOT add it to the Form ComboBox (custom drugs need free-text forms without warnings).

    NOTE on pitfall #2 (drug query re-search): The shorter brand-name text in the input does NOT trigger `handleDrugQueryChange` because `handleSelectDrug` calls `setDrugQuery()` directly, bypassing the change handler. If the user clicks back into the field and edits the text, the existing behavior (reset fields via handleDrugQueryChange) is correct -- they're modifying the drug choice.
  </action>
  <verify>
    <automated>cd /Users/furqantariq/Documents/Projects/ClinicSoftware && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Drug input shows brand name only after DB drug selection. Dropdown still shows full detail. Qty/Frequency/Duration ComboBoxes show amber border + "Custom value" hint for non-standard text. Form ComboBox has no indicator. No input is ever blocked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify drug display and amber indicator visually</name>
  <files>src/components/MedicationEntry.tsx, src/components/ComboBox.tsx</files>
  <action>
    Human verifies the two UI changes implemented in Task 1:
    1. Drug display split (brand name only after selection)
    2. Amber custom value indicator on Qty/Frequency/Duration
  </action>
  <verify>Human visual confirmation</verify>
  <done>User approves both UI changes look and behave correctly.</done>
  <what-built>
    1. Drug display split: After selecting a drug from the dropdown, the input field now shows only the brand name (e.g., "Augmentin") instead of the full detail string.
    2. Amber custom value indicator: Typing a non-standard value in Qty, Frequency, or Duration fields shows a subtle amber border with "Custom value" text.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and navigate to New Visit for any patient
    2. **Drug display test:**
       - Type "Aug" in Drug Name field
       - Verify dropdown shows full detail: "Augmentin (Amoxicillin/Clavulanate 625mg Tablet)"
       - Select the drug
       - Verify input now shows just "Augmentin" (not the full detail string)
    3. **Amber indicator test:**
       - In the Qty field, type "banana" (non-standard value)
       - Verify amber/yellow border appears around the input
       - Verify "Custom value" text appears below the input
       - Select "1" from the dropdown
       - Verify amber indicator disappears
    4. **Frequency/Duration:** Repeat amber indicator test with non-standard text
    5. **Form field:** For a custom drug (no DB selection), verify Form ComboBox does NOT show amber indicator for typed values
    6. **No blocking:** Verify all fields accept any typed text, no inputs are disabled
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npx vitest run` -- all tests pass
2. Visual verification via checkpoint: drug display + amber indicators work correctly
3. `npx tsc --noEmit` -- zero TypeScript errors
</verification>

<success_criteria>
- Drug input displays brand name only after selection from DB
- Dropdown search results show full detail (brand + salt + strength + form)
- Non-standard Qty/Frequency/Duration values trigger amber border + "Custom value" hint
- No input is ever blocked or disabled for non-standard values
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-prescription-entry-cleanup/05.1-02-SUMMARY.md`
</output>
