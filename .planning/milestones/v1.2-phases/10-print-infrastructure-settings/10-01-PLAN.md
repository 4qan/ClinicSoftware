---
phase: 10-print-infrastructure-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/printSettings.ts
  - src/components/PrintSettings.tsx
  - src/pages/SettingsPage.tsx
  - src/__tests__/printSettings.db.test.ts
  - src/__tests__/PrintSettings.test.tsx
autonomous: true
requirements: [PRSET-01, PRSET-02, PRSET-03, PRSET-04, PRENG-02]

must_haves:
  truths:
    - "Print tab appears in Settings alongside existing tabs"
    - "User can select paper size for prescription slip from A6, A5, A4, Letter"
    - "User can select paper size for dispensary slip independently"
    - "Paper size auto-saves on dropdown change (no Save button)"
    - "Missing DB keys fall back to A5 (no seeding needed)"
    - "calcMargin returns proportional margins (4mm for A6, 8mm for A5, 10mm for A4/Letter)"
  artifacts:
    - path: "src/db/printSettings.ts"
      provides: "PaperSize type, PAPER_SIZES constant, PAPER_SIZE_ORDER, PrintSettings interface, getPrintSettings(), savePrintSetting(), calcMargin()"
      exports: ["PaperSize", "PaperDimensions", "PAPER_SIZES", "PAPER_SIZE_ORDER", "PrintSettings", "getPrintSettings", "savePrintSetting", "calcMargin"]
    - path: "src/components/PrintSettings.tsx"
      provides: "Settings UI with two dropdown cards"
      exports: ["PrintSettings"]
    - path: "src/pages/SettingsPage.tsx"
      provides: "Print tab wired into pill-tab system"
      contains: "'print'"
  key_links:
    - from: "src/components/PrintSettings.tsx"
      to: "src/db/printSettings.ts"
      via: "getPrintSettings() on mount, savePrintSetting() on change"
      pattern: "getPrintSettings|savePrintSetting"
    - from: "src/pages/SettingsPage.tsx"
      to: "src/components/PrintSettings.tsx"
      via: "conditional render when activeCategory === 'print'"
      pattern: "activeCategory.*print"
---

<objective>
Create the print settings data layer and Settings UI for paper size selection.

Purpose: Establish the shared types/constants for paper sizes and let the user configure independent paper sizes for prescription and dispensary slips, persisted in IndexedDB.
Output: `src/db/printSettings.ts` (types, constants, DB functions, margin calc), `PrintSettings.tsx` component, wired into `SettingsPage.tsx` as 5th pill tab.
</objective>

<execution_context>
@/Users/furqantariq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/furqantariq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-print-infrastructure-settings/10-CONTEXT.md
@.planning/phases/10-print-infrastructure-settings/10-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor needs -->

From src/db/settings.ts (pattern to follow for DB access):
```typescript
import { db } from './index'
// Key-value: db.settings.get('key') returns { key: string, value: unknown } | undefined
// Write: db.settings.put({ key: 'keyName', value: theValue })
```

From src/pages/SettingsPage.tsx (pattern to extend):
```typescript
type SettingsCategory = 'account' | 'medications' | 'clinic' | 'data'
const TABS: { key: SettingsCategory; label: string }[] = [
  { key: 'account', label: 'Account' },
  { key: 'medications', label: 'Medications' },
  { key: 'clinic', label: 'Clinic Info' },
  { key: 'data', label: 'Data' },
]
// Conditional render: {activeCategory === 'clinic' && (<ClinicInfoSettings />)}
```

From src/components/ClinicInfoSettings.tsx (reference for settings component pattern):
- Loads data in useEffect on mount
- Saves via DB function on form submit (this plan uses auto-save on change instead)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create print settings data layer with tests</name>
  <files>src/db/printSettings.ts, src/__tests__/printSettings.db.test.ts</files>
  <behavior>
    - getPrintSettings() returns { prescriptionSize: 'A5', dispensarySize: 'A5' } when no keys exist in DB
    - After savePrintSetting('printPrescriptionSize', 'A4'), getPrintSettings() returns prescriptionSize 'A4' and dispensarySize still 'A5'
    - After savePrintSetting('printDispensarySize', 'A6'), getPrintSettings() returns dispensarySize 'A6'
    - calcMargin('A6') returns 4
    - calcMargin('A5') returns 8
    - calcMargin('A4') returns 10
    - calcMargin('Letter') returns 10
    - PAPER_SIZE_ORDER is ['A6', 'A5', 'A4', 'Letter']
    - PAPER_SIZES['A5'].label is 'A5 (148 x 210 mm)'
  </behavior>
  <action>
    Create `src/db/printSettings.ts` with:

    1. Types: `PaperSize = 'A6' | 'A5' | 'A4' | 'Letter'`, `PaperDimensions` (width, height, label), `PrintSettings` (prescriptionSize, dispensarySize)
    2. Constants: `PAPER_SIZES` record with dimensions and labels (A6: 105x148, A5: 148x210, A4: 210x297, Letter: 216x279), `PAPER_SIZE_ORDER` array ordered smallest to largest
    3. `getPrintSettings()`: reads 'printPrescriptionSize' and 'printDispensarySize' from db.settings, falls back to 'A5' if missing. Use Promise.all for parallel reads.
    4. `savePrintSetting(key, value)`: writes to db.settings.put({ key, value })
    5. `calcMargin(size)`: proportional margin calc. A5_AREA = 148*210 = 31080. ratio = area/A5_AREA. Return Math.round(Math.max(4, Math.min(10, 8 * ratio))).

    Create test file `src/__tests__/printSettings.db.test.ts` with tests for all behaviors listed above. Use resetDatabase() in beforeEach (same pattern as existing tests).
  </action>
  <verify>
    <automated>cd /Users/furqantariq/Documents/Projects/ClinicSoftware && npx vitest run src/__tests__/printSettings.db.test.ts</automated>
  </verify>
  <done>All printSettings.db tests pass. getPrintSettings defaults to A5, savePrintSetting persists correctly, calcMargin returns expected values for all four sizes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create PrintSettings component and wire into SettingsPage</name>
  <files>src/components/PrintSettings.tsx, src/pages/SettingsPage.tsx, src/__tests__/PrintSettings.test.tsx</files>
  <behavior>
    - Settings page renders a "Print" pill tab button
    - Clicking "Print" tab shows the PrintSettings component
    - PrintSettings shows heading "Print" and subtitle "Set paper sizes for each slip type. Changes apply to all future prints."
    - PrintSettings shows "Prescription Slip" card with paper size dropdown defaulting to A5
    - PrintSettings shows "Dispensary Slip" card with paper size dropdown defaulting to A5
    - Each dropdown shows 4 options: A6 (105 x 148 mm), A5 (148 x 210 mm), A4 (210 x 297 mm), Letter (216 x 279 mm)
    - Changing prescription dropdown saves to DB and updates UI without a Save button
    - Changing dispensary dropdown saves independently
  </behavior>
  <action>
    Create `src/components/PrintSettings.tsx`:
    - Load print settings on mount via getPrintSettings()
    - Two cards stacked vertically: "Prescription Slip" and "Dispensary Slip"
    - Each card has a `<select>` dropdown populated from PAPER_SIZE_ORDER/PAPER_SIZES
    - onChange calls savePrintSetting() and updates local state (auto-save, no Save button)
    - Brief subtitle under heading per user decision
    - Use same Tailwind patterns as ClinicInfoSettings (white card, border, rounded-lg, p-6)

    Update `src/pages/SettingsPage.tsx`:
    - Add 'print' to SettingsCategory union type
    - Add { key: 'print', label: 'Print' } to TABS array
    - Add conditional render block: {activeCategory === 'print' && (<PrintSettings />)}
    - Import PrintSettings component

    Create `src/__tests__/PrintSettings.test.tsx`:
    - Test Print tab appears in settings
    - Test PrintSettings renders with both dropdowns
    - Test dropdown defaults to A5
    - Test changing prescription size saves to DB
    - Test changing dispensary size saves independently
    - Use render with MemoryRouter wrapping SettingsPage (follows existing test patterns)
  </action>
  <verify>
    <automated>cd /Users/furqantariq/Documents/Projects/ClinicSoftware && npx vitest run src/__tests__/PrintSettings.test.tsx</automated>
  </verify>
  <done>Print tab visible in Settings. Both dropdowns render with A5 default. Changing a dropdown persists the value to IndexedDB. Tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/printSettings.db.test.ts` -- data layer tests green
- `npx vitest run src/__tests__/PrintSettings.test.tsx` -- UI component tests green
- `npx vitest run` -- full suite still passes (no regressions)
</verification>

<success_criteria>
- Print tab appears as 5th pill tab in Settings page
- Two independent paper size dropdowns (prescription + dispensary) with A6/A5/A4/Letter options
- Labels include dimensions: "A5 (148 x 210 mm)" format
- Auto-save on change, no Save button
- Default A5 when no DB entry exists
- calcMargin() exported and tested for all four paper sizes
- All new + existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-print-infrastructure-settings/10-01-SUMMARY.md`
</output>
