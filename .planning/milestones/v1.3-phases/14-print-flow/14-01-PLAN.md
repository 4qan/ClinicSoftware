---
phase: 14-print-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/PrintVisitPage.tsx
  - src/pages/NewVisitPage.tsx
  - src/components/MedicationList.tsx
  - src/__tests__/PrintVisitPage.keyboard.test.tsx
autonomous: true
requirements:
  - PRNT-01
  - PRNT-02
  - PRNT-03

must_haves:
  truths:
    - "User can Tab from the last prescription field to Save & Print without stopping at medication remove buttons"
    - "User can press Enter on the Print button to open the browser print dialog"
    - "After the print dialog closes (Enter or Escape), focus returns to the Print button"
    - "On PrintVisitPage, Tab lands on the Print button immediately (toggle tabs skipped)"
    - "Print button is auto-focused when PrintVisitPage loads"
  artifacts:
    - path: "src/pages/PrintVisitPage.tsx"
      provides: "Print button ref, autoFocus, afterprint focus restore, tabIndex={-1} on toggle tabs"
      contains: "printButtonRef"
    - path: "src/pages/NewVisitPage.tsx"
      provides: "Navigate with ?auto=prescription query param"
      contains: "auto=prescription"
    - path: "src/components/MedicationList.tsx"
      provides: "tabIndex={-1} on both Remove buttons"
      contains: "tabIndex={-1}"
    - path: "src/__tests__/PrintVisitPage.keyboard.test.tsx"
      provides: "PRNT-01, PRNT-02, PRNT-03 test coverage"
      min_lines: 30
  key_links:
    - from: "src/pages/NewVisitPage.tsx"
      to: "src/pages/PrintVisitPage.tsx"
      via: "navigate with ?auto=prescription"
      pattern: "auto=prescription"
    - from: "src/pages/PrintVisitPage.tsx handleAfterPrint"
      to: "printButtonRef"
      via: "focus() call in afterprint handler"
      pattern: "printButtonRef\\.current\\??\\.focus"
---

<objective>
Wire the keyboard-driven print flow: clean tab path to Print button, auto-print on Save & Print, and focus restore after the print dialog closes.

Purpose: Complete the v1.3 keyboard milestone. The doctor finishes a prescription, Tabs to Save & Print, presses Enter, print dialog opens, confirms with Enter. Zero mouse interaction needed.
Output: Three modified source files, one new test file.
</objective>

<execution_context>
@/Users/furqantariq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/furqantariq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-print-flow/14-CONTEXT.md
@.planning/phases/14-print-flow/14-RESEARCH.md

<interfaces>
<!-- Key integration points. All changes are surgical additions to existing files. -->

From src/pages/PrintVisitPage.tsx (current state):
- Line 56: `const autoPrintTimer = useRef<ReturnType<typeof setTimeout> | null>(null)` — existing ref pattern
- Line 98: `autoPrintTimer.current = setTimeout(() => window.print(), 200)` — auto-print already works
- Line 109-112: `handleAfterPrint` callback — currently resets printMode and removes page style, needs focus restore added
- Line 173-194: Two toggle buttons (Prescription/Dispensary) — need `tabIndex={-1}`
- Line 204-210: Print button — needs ref, autoFocus

From src/pages/NewVisitPage.tsx:
- Line 201: `navigate(`/visit/${visitId}/print`)` — needs `?auto=prescription` appended

From src/components/MedicationList.tsx:
- Line 56: Desktop Remove button — needs `tabIndex={-1}`
- Line 85: Mobile Remove button — needs `tabIndex={-1}`

From src/components/Breadcrumbs.tsx:
- Line 23: Already has `tabIndex={-1}` on links — no change needed
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write keyboard tests for PRNT-01, PRNT-02, PRNT-03</name>
  <files>src/__tests__/PrintVisitPage.keyboard.test.tsx</files>
  <behavior>
    - PRNT-01: Toggle tab buttons (Prescription/Dispensary) have tabIndex={-1} and are skipped during tab navigation
    - PRNT-01: Print button has autoFocus and receives focus on mount
    - PRNT-02: Pressing Enter on focused Print button calls window.print (via the click handler)
    - PRNT-03: After afterprint event fires, document.activeElement is the Print button
    - PRNT-03: Focus restore works the same whether auto-print or manual print triggered the dialog
  </behavior>
  <action>
    Create `src/__tests__/PrintVisitPage.keyboard.test.tsx` with tests covering all three PRNT requirements.

    Copy the test setup pattern (mocks, renderPrintPage helper, beforeEach/afterEach) from existing `src/__tests__/PrintVisitPage.test.tsx`.

    Test structure:
    1. "toggle tab buttons have tabIndex={-1}" — render page, query both toggle buttons, assert tabIndex === -1
    2. "Print button receives autoFocus" — render page, await load, assert document.activeElement is the Print button
    3. "Enter on Print button calls window.print" — spy on window.print, focus Print button, fire Enter via userEvent.keyboard('{Enter}'), assert window.print was called
    4. "focus returns to Print button after afterprint event" — render page, dispatch `new Event('afterprint')` on window, assert document.activeElement is the Print button
    5. "focus returns to Print button after auto-print afterprint" — render with ?auto=prescription, dispatch afterprint, assert focus on Print button

    Mock `window.print` with `vi.spyOn(window, 'print').mockImplementation(() => {})` to prevent actual print dialogs.

    All tests should FAIL initially (Red phase) because the production code changes have not been made yet.
  </action>
  <verify>
    <automated>cd /Users/furqantariq/Documents/Projects/ClinicSoftware && npx vitest run src/__tests__/PrintVisitPage.keyboard.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists with 5 tests. Tests fail because production code lacks autoFocus, tabIndex={-1}, printButtonRef, and focus restore logic.</done>
</task>

<task type="auto">
  <name>Task 2: Wire tab path, autoFocus, auto-print param, and focus restore</name>
  <files>src/pages/PrintVisitPage.tsx, src/pages/NewVisitPage.tsx, src/components/MedicationList.tsx</files>
  <action>
    Five surgical changes across three files. All use existing patterns (tabIndex={-1}, useRef, autoFocus).

    **1. NewVisitPage.tsx line 201** — Add `?auto=prescription` to navigate call:
    ```
    navigate(`/visit/${visitId}/print?auto=prescription`)
    ```
    This triggers the existing auto-print logic on PrintVisitPage (already reads `searchParams.get('auto')`).

    **2. MedicationList.tsx lines 56 and 85** — Add `tabIndex={-1}` to BOTH Remove buttons (desktop table row and mobile card layout). This removes them from the tab sequence so Tab jumps from the last prescription field to Save & Print.

    **3. PrintVisitPage.tsx toggle buttons (lines 173, 184)** — Add `tabIndex={-1}` to both Prescription and Dispensary toggle buttons. Tab skips them and lands on Print button.

    **4. PrintVisitPage.tsx Print button (line 204)** — Add a ref and autoFocus:
    - Create `const printButtonRef = useRef<HTMLButtonElement>(null)` at the top of the component (near existing `autoPrintTimer` ref)
    - Add `ref={printButtonRef}` and `autoFocus` to the Print button element

    **5. PrintVisitPage.tsx handleAfterPrint (line 109-112)** — Add focus restore:
    ```
    const handleAfterPrint = useCallback(() => {
      setPrintMode(null)
      removePageStyle()
      printButtonRef.current?.focus()
    }, [])
    ```
    This ensures focus returns to Print button after the browser print dialog closes, whether the user printed or cancelled.

    No new imports needed. `useRef` is already imported in both files that use it.
  </action>
  <verify>
    <automated>cd /Users/furqantariq/Documents/Projects/ClinicSoftware && npx vitest run src/__tests__/PrintVisitPage.keyboard.test.tsx && npx vitest run src/__tests__/PrintVisitPage.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 keyboard tests pass (Green phase). Existing PrintVisitPage tests still pass. Navigate includes ?auto=prescription. Both MedicationList Remove buttons have tabIndex={-1}. Toggle tabs have tabIndex={-1}. Print button has autoFocus and ref. handleAfterPrint calls focus().</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/__tests__/PrintVisitPage.keyboard.test.tsx` — all PRNT tests green
2. `npx vitest run src/__tests__/PrintVisitPage.test.tsx` — existing tests still pass
3. `npx vitest run` — full suite green (no regressions)
</verification>

<success_criteria>
- Tab from last prescription field reaches Save & Print without stopping at Remove buttons (PRNT-01)
- Toggle tabs on PrintVisitPage are skipped in tab order (PRNT-01)
- Print button is auto-focused on PrintVisitPage load (PRNT-01)
- Enter on Print button triggers window.print (PRNT-02)
- Focus returns to Print button after afterprint event (PRNT-03)
- Save & Print navigates with ?auto=prescription for auto-print (existing behavior enhanced)
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/14-print-flow/14-01-SUMMARY.md`
</output>
