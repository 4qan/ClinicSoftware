---
phase: 12-focus-foundation-and-form-submission
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Sidebar.tsx
  - src/components/Header.tsx
  - src/components/Breadcrumbs.tsx
  - src/components/Toast.tsx
  - src/pages/NewVisitPage.tsx
  - src/__tests__/tab-order.test.tsx
autonomous: true
requirements:
  - FOCUS-03
  - FORM-01
  - FORM-02
  - FORM-03

must_haves:
  truths:
    - "Tab moves through form fields in visual order on all critical-path pages"
    - "Sidebar nav links, header links, breadcrumb links, and toast close buttons are not reachable via Tab"
    - "Pressing Enter on the login form submits it"
    - "Pressing Enter on the patient creation form submits it"
    - "NewVisitPage has no form wrapper (Enter creates newlines in textarea, not submission)"
    - "Action buttons in NewVisitPage are ordered: Save & Print, Save Visit, Cancel"
  artifacts:
    - path: "src/components/Sidebar.tsx"
      provides: "tabIndex={-1} on nav links and logout button"
      contains: "tabIndex={-1}"
    - path: "src/components/Header.tsx"
      provides: "tabIndex={-1} on home link, settings link, logout button (NOT search input)"
      contains: "tabIndex={-1}"
    - path: "src/components/Breadcrumbs.tsx"
      provides: "tabIndex={-1} on all breadcrumb links"
      contains: "tabIndex={-1}"
    - path: "src/components/Toast.tsx"
      provides: "tabIndex={-1} on close button"
      contains: "tabIndex={-1}"
    - path: "src/pages/NewVisitPage.tsx"
      provides: "Action buttons reordered: Save & Print first"
    - path: "src/__tests__/tab-order.test.tsx"
      provides: "Tests for tabIndex and form submission"
  key_links:
    - from: "src/components/Sidebar.tsx"
      to: "tab order"
      via: "tabIndex={-1} on Link elements"
      pattern: "tabIndex=\\{-1\\}"
    - from: "src/pages/NewVisitPage.tsx"
      to: "button order in DOM"
      via: "JSX order of action buttons"
      pattern: "Save.*Print.*Save Visit.*Cancel"
---

<objective>
Fix tab order by removing non-critical-path elements from the tab sequence, reorder action buttons in NewVisitPage, and verify form submission behavior.

Purpose: Tab navigates only through critical-path form fields in logical order. Non-essential chrome (sidebar, header nav, breadcrumbs, toasts) is removed from keyboard flow. Form Enter-to-submit works where intended.
Output: Tab order fixes in 4 components, button reorder in NewVisitPage, tab-order and form-submit tests.
</objective>

<execution_context>
@/Users/furqantariq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/furqantariq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-focus-foundation-and-form-submission/12-CONTEXT.md
@.planning/phases/12-focus-foundation-and-form-submission/12-RESEARCH.md

@src/components/Sidebar.tsx
@src/components/Header.tsx
@src/components/Breadcrumbs.tsx
@src/components/Toast.tsx
@src/pages/NewVisitPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tab order fixes and form submission tests</name>
  <files>src/components/Sidebar.tsx, src/components/Header.tsx, src/components/Breadcrumbs.tsx, src/components/Toast.tsx, src/pages/NewVisitPage.tsx, src/__tests__/tab-order.test.tsx</files>
  <behavior>
    - Test: Sidebar nav Link elements have tabIndex={-1}
    - Test: Sidebar logout button has tabIndex={-1}
    - Test: Sidebar collapse toggle button does NOT have tabIndex={-1} (must remain tabbable)
    - Test: Header home link, settings link, and logout button have tabIndex={-1}
    - Test: Header search input does NOT have tabIndex={-1} (must remain tabbable)
    - Test: Breadcrumb Link elements have tabIndex={-1}
    - Test: Toast close button has tabIndex={-1}
    - Test: NewVisitPage action buttons appear in DOM order: "Save & Print" before "Save Visit" before "Cancel"
    - Test: LoginPage form calls onSubmit when Enter pressed (check existing login.test.tsx, extend if needed)
    - Test: PatientRegistrationForm calls onSubmit when Enter pressed (check existing registration.test.tsx, extend if needed)
  </behavior>
  <action>
1. Create `src/__tests__/tab-order.test.tsx` with tests for all the behaviors listed above. Use @testing-library/react to render each component and assert tabIndex values and DOM order.

   For Sidebar: render with required router context (MemoryRouter). Assert nav links and logout button have `tabIndex: -1`. Assert the collapse toggle button does NOT have `tabIndex: -1`.

   For Header: render with router context. Assert home link, settings link, logout button have `tabIndex: -1`. Assert search input does NOT have `tabIndex: -1`.

   For Breadcrumbs: render with router context and sample crumbs. Assert all Link elements have `tabIndex: -1`.

   For Toast: render a toast. Assert close button has `tabIndex: -1`.

   For NewVisitPage button order: render with required context. Find all buttons in the action bar area. Assert "Save & Print" appears before "Save Visit" which appears before "Cancel" in DOM order. (May need to mock router and db context.)

   For form submission (FORM-01, FORM-02): check existing `login.test.tsx` and `registration.test.tsx`. If they already test Enter-key submission, no changes needed. If not, add a test in each that simulates pressing Enter on an input field and asserts the submit handler is called.

   For FORM-03: NewVisitPage has no `<form>` wrapper by design. Add a test or comment confirming this is intentional (the textarea needs Enter for newlines). No code change needed.

2. Apply the tab order fixes to make tests pass:

   **Sidebar.tsx:** Add `tabIndex={-1}` to every `<Link>` in the navItems map and to the logout `<button>`. Do NOT add it to the collapse toggle button.

   **Header.tsx:** Add `tabIndex={-1}` to the home `<Link>`, settings `<Link>`, and logout `<button>`. Do NOT add it to the search `<input>` (patient search is critical path).

   **Breadcrumbs.tsx:** Add `tabIndex={-1}` to every `<Link>` element.

   **Toast.tsx:** Add `tabIndex={-1}` to the close `<button>`.

   **NewVisitPage.tsx:** Reorder the action buttons in JSX so DOM order is:
   1. Save & Print button (first in tab order)
   2. Save Visit button (second)
   3. Cancel button (last)
   Currently the order is Cancel, Save Visit, Save & Print. Swap the JSX order. Keep all onClick handlers, classNames, and conditional rendering intact. Only change DOM position.

3. Run tests to confirm GREEN.
  </action>
  <verify>
    <automated>npm test -- src/__tests__/tab-order.test.tsx src/__tests__/login.test.tsx src/__tests__/registration.test.tsx --reporter=dot</automated>
  </verify>
  <done>All sidebar nav links, header nav links, breadcrumb links, and toast close buttons have tabIndex={-1}. Sidebar toggle and header search input remain tabbable. NewVisitPage buttons ordered Save & Print > Save Visit > Cancel. Login and patient registration forms submit on Enter. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite verification</name>
  <files></files>
  <action>
Run the full test suite and build to confirm no regressions from the tab order changes and button reorder.

If any existing tests break due to the tabIndex additions (e.g., tests that query buttons without considering tabIndex), fix the test assertions to accommodate the new tabIndex values.
  </action>
  <verify>
    <automated>npm run build && npm test -- --reporter=dot</automated>
  </verify>
  <done>Build succeeds. Full test suite passes. No regressions from tab order changes.</done>
</task>

</tasks>

<verification>
- `npm test -- src/__tests__/tab-order.test.tsx` passes (tabIndex, button order, form submit tests)
- `npm run build` succeeds
- `npm test` full suite passes
</verification>

<success_criteria>
- Sidebar nav links and logout button have tabIndex={-1}; collapse toggle does not
- Header links and logout have tabIndex={-1}; search input does not
- Breadcrumb links have tabIndex={-1}
- Toast close button has tabIndex={-1}
- NewVisitPage buttons ordered: Save & Print, Save Visit, Cancel
- Login form submits on Enter (existing behavior verified)
- Patient registration form submits on Enter (existing behavior verified)
- NewVisitPage intentionally has no form wrapper (verified by test/comment)
- Build and full test suite pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-focus-foundation-and-form-submission/12-02-SUMMARY.md`
</output>
